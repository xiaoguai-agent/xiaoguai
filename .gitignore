# Rust
target/
**/*.rs.bk
Cargo.lock.bak
*.pdb

# Cargo: keep workspace Cargo.lock, ignore per-crate
crates/*/Cargo.lock

# Node / pnpm
node_modules/
**/dist/
**/build/
.pnpm-store/
*.tsbuildinfo
.eslintcache

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Env / secrets
.env
.env.*
!.env.example
!.env.local.example
secrets/
*.pem
*.key
*.p12

# Logs + temp
*.log
logs/
tmp/
.tmp/

# DB
*.db
*.sqlite
*.sqlite3

# SBOM / signatures generated locally (release CI handles real ones)
sbom-*.json
*.sig
*.bundle

# Docs build
docs/_build/
docs/.docusaurus/
docs/book/book/

# Local model files (huge)
models/
*.gguf
*.safetensors
*.bin
.env.local

# Agent worktrees (transient; created by Claude Code isolation runs)
.claude/worktrees/

# Local-only dev tuning (incremental=false to avoid 211G target blowout, see ci-gotchas)
.cargo/config.toml

# Local presentation assets (not part of the repo)
ppt/
