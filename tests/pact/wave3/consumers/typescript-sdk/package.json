{
  "name": "@xiaoguai/pact-consumer-typescript-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Pact consumer contract tests for the TypeScript SDK against xiaoguai wave-3 API",
  "scripts": {
    "test": "jest --testPathPattern=pact",
    "pact:test": "jest --testPathPattern=pact --forceExit"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.1.3",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
