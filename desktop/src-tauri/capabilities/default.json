{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the floating chat window. The global shortcut is registered Rust-side (no JS permission needed); the window only needs to listen to the Rust-emitted `chat://event` and focus events, plus invoke the chat commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus"
  ]
}
