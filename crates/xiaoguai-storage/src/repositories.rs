//! Repository pattern: business code only sees these traits, never raw SQL.

// TODO(v0.5): TenantRepository, UserRepository, SessionRepository,
// MessageRepository, ToolCallRepository, AuditRepository, McpRegistryRepository.
