apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "xiaoguai.configmapName" . }}
  labels:
    {{- include "xiaoguai.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
