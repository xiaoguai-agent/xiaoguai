{# rebalance-proposal-card.md.j2
   Rendered by capacity-rebalancer for human review before any account moves.
   Delivered via slack-revops-channel and admin-ui HOTL queue.
   Variables:
     proposal    — RebalanceProposal (see capacity-rebalancer system prompt)
     expires_at  — ISO-8601 (approval deadline, hotl.approval_expiry_hours from now)
#}
## Rebalance Proposal — {{ proposal.territory_name }}

> **Action required:** Approve or reject by {{ expires_at | format_datetime }}.
> No accounts will be moved without your explicit approval.

---

### Current vs. Projected Load

| Metric | Current | After Rebalance |
|--------|--------:|----------------:|
| Imbalance (max − min) | {{ "%.1f" | format(proposal.current_imbalance_pct) }}% | {{ "%.1f" | format(proposal.projected_imbalance_pct) }}% |

**Confidence:** {{ proposal.confidence | title }}

{{ proposal.explanation }}

---

### Proposed Transfers ({{ proposal.transfers | length }})

{% for t in proposal.transfers %}
#### Transfer {{ loop.index }}: {{ t.account_name }}

| Field | Value |
|-------|-------|
| From | {{ t.from_rep_name }} |
| To | {{ t.to_rep_name }} |
| Estimated ARR | {{ t.arr_estimate | currency | default("—") }} |

**Rationale:** {{ t.rationale }}

{% else %}
*No transfers proposed — all accounts are either too new or too high-value to move safely.*
{% endfor %}

---

**Approve all transfers:** Reply `approve` in this thread or use the admin UI.
**Reject:** Reply `reject` with optional reason.
**Approve partially:** Specify transfer numbers, e.g. `approve 1 3`.

*Generated by capacity-rebalancer · proposal will expire if not acted on.*
