{# postmortem-stub.md.j2 — minimal postmortem skeleton created at incident open time.
   Written as the initial Statuspage incident body and optionally committed to the
   incidents/ directory in the engineering repo for engineers to fill in later.

   This is intentionally sparse: the on-call engineer owns the final postmortem.
   The stub records machine-generated facts; human analysis is added during review.

   Context variables:
     alert      — normalized Alert struct
     classify   — ClassifiedAlert output
     lookup     — RunbookMatch output
     snapshot   — ObservabilitySnapshot output
     pack_version — string #}
# Postmortem Stub — {{ alert.name }}

> **Status:** OPEN — _to be completed by on-call engineer_
> **Incident ID:** `{{ alert.id }}`
> **Priority:** {{ classify.priority }}
> **Component:** {{ classify.suspected_component }}
> **Environment:** {{ alert.environment | default("unknown") }}
> **Alert fired:** {{ alert.fired_at }}
> **Generated by:** xiaoguai devops-oncall v{{ pack_version }}

---

## 1. Summary

_Fill in after resolution._

> Machine-generated impact note: {{ classify.impact_summary }}

## 2. Timeline

| Time (UTC) | Event |
|---|---|
| `{{ alert.fired_at }}` | Alert fired — {{ alert.name }} |
| `<!-- time -->` | On-call engineer acknowledged |
| `<!-- time -->` | Root cause identified |
| `<!-- time -->` | Mitigation applied |
| `<!-- time -->` | Incident resolved |

## 3. Root Cause

_Fill in after investigation._

**Observability summary at time of alert:**
{{ snapshot.summary }}

{% if classify.similar_incidents | length > 0 -%}
**Similar past incidents (may share root cause):**
{% for inc in classify.similar_incidents -%}
- {{ inc.title }} ({{ inc.resolved_at | default("unresolved") }})
{% endfor %}
{%- endif %}

## 4. Impact

_Quantify customer impact: affected users, error rate, duration._

## 5. Runbook Used

**{{ lookup.runbook_title }}** (id: `{{ lookup.runbook_id }}`, match score: {{ lookup.match_score | round(2) }})
{% if lookup.fallback %}
> Fallback generic runbook — no specific runbook was found for this alert.
{% endif %}

## 6. Action Items

| Priority | Owner | Action | Due |
|---|---|---|---|
| <!-- P0/P1/P2 --> | <!-- team --> | <!-- what --> | <!-- date --> |

## 7. Prevention

_What systemic change would prevent recurrence?_

---

*This stub was auto-generated at alert time.  Complete sections 1, 4, 6, 7 after resolution.*
*Reviewed and signed off by: <!-- @engineer -->*
*Date closed: <!-- YYYY-MM-DD -->*
