{# split-proposal.md.j2
   Rendered by card-split-execute output and sent to the HotL approver channel.
   Contains the full split plan and Approve / Reject interactive buttons. #}
*[HotL] Split approval required* :scissors:

A card has been flagged as oversized and a split has been proposed.
Please review and approve or reject below.

---

*Original card*: {{ card.title | truncate(120) }}
*Estimated effort*: `{{ split.original_est_effort }}` (basis: `{{ split.effort_basis }}`)
*Urgency*: `{{ triage.urgency | upper }}`
*Owner hint*: {{ triage.owner_hint | default('_TBD_') }}

---

*Proposed sub-cards ({{ split.split_cards | length }}):*

{% for sub in split.split_cards %}
{{ loop.index }}. *{{ sub.title }}*
   Effort: `{{ sub.est_effort }}`  |  Labels: {{ sub.labels | join(', ') | default('(none)') }}
   {{ sub.description | truncate(200) }}
{% if sub.depends_on_index is not none %}
   _Depends on sub-card {{ sub.depends_on_index + 1 }}_
{% endif %}

{% endfor %}

---

*Rationale*: {{ split.rationale | truncate(200) }}

*Proposal expires in {{ hotl_timeout_hours }}h if no action is taken.*

{% if not kanban_enabled %}
> _Note: kanban subsystem not yet active. Approving will persist the proposal;
> it will be executed automatically when XIAOGUAI_KANBAN_ENABLED=true (v1.4)._
{% endif %}

_Generated by xiaoguai kanban-automation v{{ pack_version }}_
