{# drill-down-table.md.j2
   Jinja2 template for the budget rollup drill-down table.
   Rendered by the budget-rollup agent.
   Variables: period_label, rollup (list of dicts per cost-centre+category),
   ytd (list of dicts).
   YTD rows flagged with "[!]" when ytd_variance_pct > 5%.
#}
## Budget Rollup — {{ period_label }}

**Prepared**: {{ now | datetime }}  |  **Status**: Pending CFO Approval

---

### Current Period

| Cost Centre | Category | Actual | Budget | Variance | Var % |
|-------------|----------|:------:|:------:|:--------:|:-----:|
{% for row in rollup | sort(attribute="cost_centre") %}
| {{ row.cost_centre }} | {{ row.category | title }} | {{ row.actual_total | currency }} | {{ row.budget_total | currency }} | {{ row.variance | currency }} | {{ row.variance_pct }}% |
{% endfor %}
| **TOTAL** | | **{{ rollup | sum(attribute="actual_total") | currency }}** | **{{ rollup | sum(attribute="budget_total") | currency }}** | **{{ rollup | sum(attribute="variance") | currency }}** | |

---

### Year-to-Date

| Cost Centre | Category | YTD Actual | YTD Budget | YTD Variance | YTD Var % | |
|-------------|----------|:----------:|:----------:|:------------:|:---------:|:-:|
{% for row in ytd | sort(attribute="cost_centre") %}
| {{ row.cost_centre }} | {{ row.category | title }} | {{ row.ytd_actual | currency }} | {{ row.ytd_budget | currency }} | {{ row.ytd_variance | currency }} | {{ row.ytd_variance_pct }}% | {% if row.ytd_variance_pct | float > 5 %}[!]{% endif %} |
{% endfor %}
| **YTD TOTAL** | | **{{ ytd | sum(attribute="ytd_actual") | currency }}** | **{{ ytd | sum(attribute="ytd_budget") | currency }}** | **{{ ytd | sum(attribute="ytd_variance") | currency }}** | | |

`[!]` = YTD actual exceeds YTD budget by more than 5%

---

*Generated by xiaoguai finance-fpa pack v{{ pack_version }}.*
*All figures in {{ currency }}. Approve in Xiaoguai admin UI or Slack.*
