# Brand Mention Digest — {{ digest_date }}

> Generated by xiaoguai media-monitoring pack v{{ pack_version }}
> Period: {{ period_start }} → {{ period_end }} UTC

---

## Summary

| Metric | Value | vs. Yesterday |
|---|---|---|
| Total mentions | {{ stats.total_mentions | commify }} | {{ stats.total_mentions_delta | signed_pct }} |
| Positive | {{ stats.positive_count | commify }} ({{ stats.positive_ratio | pct }}) | {{ stats.positive_ratio_delta | signed_pct }} |
| Neutral | {{ stats.neutral_count | commify }} ({{ stats.neutral_ratio | pct }}) | — |
| Negative | {{ stats.negative_count | commify }} ({{ stats.negative_ratio | pct }}) | {{ stats.negative_ratio_delta | signed_pct }} |
| Influence-weighted reach | {{ stats.influence_weighted_reach | commify }} | {{ stats.reach_delta | signed_pct }} |

---

## Sentiment Breakdown by Channel

| Channel | Mentions | Positive | Neutral | Negative |
|---|---|---|---|---|
{% for ch in channel_stats %}
| {{ ch.channel | title }} | {{ ch.total | commify }} | {{ ch.positive_ratio | pct }} | {{ ch.neutral_ratio | pct }} | {{ ch.negative_ratio | pct }} |
{% endfor %}

---

## Narrative Themes

{% for cluster in narrative_clusters %}
### {{ loop.index }}. {{ cluster.theme }} — {{ cluster.trajectory | upper }}

{{ cluster.summary }}

**Sentiment**: {{ cluster.sentiment.positive | pct }} positive / {{ cluster.sentiment.neutral | pct }} neutral / {{ cluster.sentiment.negative | pct }} negative
**Active on**: {{ cluster.top_channels | join(', ') }}
**Mentions in cluster**: {{ cluster.mention_count | commify }}

**Representative quotes**:
{% for quote in cluster.representative_quotes %}
> "{{ quote }}"
{% endfor %}

---
{% endfor %}

## Top Mentions by Reach

| Channel | Reach | Sentiment | Excerpt | URL |
|---|---|---|---|---|
{% for m in top_mentions %}
| {{ m.channel | title }} | {{ m.reach | commify }} | {{ m.sentiment }} | {{ m.body | truncate(80) }} | [link]({{ m.url }}) |
{% endfor %}

---

## Emerging Keywords

{% if emergent_keywords %}
| Keyword | Lift | Negative Ratio | Top Channel |
|---|---|---|---|
{% for kw in emergent_keywords %}
| {{ kw.keyword }} | {{ kw.lift | round(1) }}x | {{ kw.negative_ratio | pct }} | {{ kw.top_channel }} |
{% endfor %}
{% else %}
No significant keyword emergence detected in this period.
{% endif %}

---

## Crisis Events (Past 24h)

{% if crisis_events %}
{% for ce in crisis_events %}
### [{{ ce.severity | upper }}] {{ ce.title }}

- **Declared**: {{ ce.declared_at }}
- **Status**: {{ ce.status | title }}
- **Trigger**: {{ ce.trigger_type }}
- **Channels**: {{ ce.channels | join(', ') }}
- **Hourly mentions at peak**: {{ ce.hourly_mention_count | commify }}
- **Influence-weighted reach**: {{ ce.influence_weighted_reach | commify }}
{% if ce.resolved_at %}
- **Resolved**: {{ ce.resolved_at }}
{% endif %}

{{ ce.narrative }}

{% endfor %}
{% else %}
No crisis events declared in this period.
{% endif %}

---

*This digest is auto-generated. To provide sentiment feedback, visit {{ feedback_url }}.*
*To unsubscribe or adjust digest frequency, update your media-monitoring config.*
