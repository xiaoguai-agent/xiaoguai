{# Incident Impact Frame Template
   Rendered by incident-impact-evaluator for Slack alert and audit archive.
   Variables: vendor, incident, impacted_systems
#}
# Vendor Incident Alert — {{ vendor.name }}

**Detected:** {{ incident.detected_at | datetime }}
**Severity:** {{ incident.severity | upper }}
**Source:** {{ incident.source }}
{% if incident.status_page_url %}
**Status page:** {{ incident.status_page_url }}
{% endif %}

---

## Blast Radius Score: {{ incident.blast_radius_score | round(0) | int }} / 100

{% if incident.blast_radius_score <= 20 %}
**Impact: MINIMAL** — Peripheral tooling; no customer-facing impact expected.
{% elif incident.blast_radius_score <= 50 %}
**Impact: MODERATE** — Internal workflow disruption; engineering or ops teams affected.
{% elif incident.blast_radius_score <= 80 %}
**Impact: HIGH** — Core platform dependency; potential customer-facing degradation.
{% else %}
**Impact: CRITICAL** — Critical path; active customer impact or revenue risk.
{% endif %}

---

## Impacted Internal Systems

{% if impacted_systems %}
| System | Customer-Facing | Owning Team |
|--------|:---------------:|-------------|
{% for sys in impacted_systems %}
| {{ sys.name }} | {{ 'Yes' if sys.customer_facing else 'No' }} | {{ sys.team | default('Unknown') }} |
{% endfor %}
{% else %}
No integration map available for this vendor. Manual assessment recommended.
{% endif %}

---

## Impact Narrative

{{ incident.impact_narrative }}

---

## Vendor Context

| Field | Value |
|-------|-------|
| Category | {{ vendor.category }} |
| Tier | {{ vendor.tier }} |
| Risk score | {{ vendor.risk_score | default('N/A') }} |
| Annual spend | ${{ vendor.annual_spend_usd | format_currency }} |

---

{% if incident.resolved_at %}
**RESOLVED** at {{ incident.resolved_at | datetime }}
{% else %}
**ONGOING** — Monitoring for resolution.
{% endif %}

*Generated by xiaoguai vendor-management pack — incident-impact-evaluator agent.*
