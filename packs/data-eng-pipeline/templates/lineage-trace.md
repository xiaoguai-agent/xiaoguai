# Lineage Trace — {{ dataset_id }}

**Traced at**: {{ now | datetime }}
**Trigger**: {{ trigger_event }} (breach: {{ breach_id }})

---

## Lineage Path (upstream → downstream)

```
{{ lineage_path_ascii }}
```

{% if furthest_upstream_failure %}
## Furthest Upstream Failure

| Field | Value |
|-------|-------|
| Node | `{{ furthest_upstream_failure.node_id }}` |
| Type | {{ furthest_upstream_failure.edge_kind }} |
| Status | {{ furthest_upstream_failure.status }} |
| Error | {{ furthest_upstream_failure.error_message | default("none recorded") }} |
| Last run | {{ furthest_upstream_failure.last_run_at | datetime }} |

**Interpretation**: The root failure originated at `{{ furthest_upstream_failure.node_id }}` and
cascaded downstream through {{ cascade_depth }} hop(s) to reach `{{ dataset_id }}`.
{% else %}
## No Upstream Failure Detected

The lineage graph shows no upstream failures. The stale dataset may be caused by:
- A missing scheduled run (DAG not triggered).
- A source-side delay not visible in the lineage graph.
- A freshness check misconfiguration (check `sla_max_age_minutes`).
{% endif %}

---

## Breadcrumbs (most recent run per node)

{% for node in lineage_nodes %}
### `{{ node.node_id }}`
- **Status**: {{ node.status }}
- **Row count**: {{ node.row_count | default("unknown") }}
- **Duration**: {{ node.duration_seconds | default("unknown") }}s
- **Recorded**: {{ node.recorded_at | datetime }}
{% if node.breadcrumb %}
- **Anomaly flags**: {{ node.breadcrumb.anomaly_flags | default("none") }}
{% endif %}
{% endfor %}

---

*Generated by xiaoguai lineage-breadcrumb-writer + freshness-investigator.*
