# SLA Breach Summary — {{ dataset_id }}

**Status**: {{ status | upper }}
**Pipeline**: `{{ pipeline_id }}`
**Dataset**: `{{ dataset_id }}`
**SLA window**: {{ sla_max_age_minutes }} minutes
**Current age**: {{ age_minutes | round(0) }} minutes ({{ (age_minutes / sla_max_age_minutes * 100) | round(0) }}% of SLA)
**Breach started**: {{ breach_started_at | datetime }}

---

## Root Cause

{{ root_cause | default("Investigation in progress — check back in a few minutes.") }}

---

## Impact

- Downstream consumers of `{{ dataset_id }}` may be reading stale data.
- Affected pipelines (from lineage): {{ affected_downstream | join(", ") | default("unknown — lineage not yet traced") }}

---

## Recommended Action

{{ suggested_action | default("1. Check Airflow for the latest DAG run status.\n2. Review freshness-investigator findings in the Admin UI.\n3. If transient: approve retry via Admin UI.") }}

---

## Links

- [Freshness breach in Admin UI]({{ admin_url }}/data-eng/breaches/{{ breach_id }})
- [Pipeline lineage graph]({{ admin_url }}/data-eng/lineage/{{ pipeline_id }})
- [Investigation transcript]({{ admin_url }}/data-eng/investigations/{{ investigation_id | default("pending") }})

---

*Generated by xiaoguai data-eng-pipeline pack at {{ now | datetime }}.*
