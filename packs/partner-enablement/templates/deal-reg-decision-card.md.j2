{# packs/partner-enablement/templates/deal-reg-decision-card.md.j2
   Deal-registration routing decision card.
   Rendered by deal-reg-router and posted to the channel manager via
   outputs/slack-channel-team.yaml and outputs/partner-portal-decision-post-back.yaml.
#}
# Deal Registration Decision — {{ deal_reg.account_name }}

> **{{ routing_decision | upper }}** — {{ routed_at | datetimeformat }}

**Partner:** {{ partner.name }} (Tier: {{ partner.tier | upper }})
**Account:** {{ deal_reg.account_name }}{% if deal_reg.account_domain %} · `{{ deal_reg.account_domain }}`{% endif %}
**Estimated ARR:** {{ deal_reg.estimated_arr | default('—') | currency }}
**Close Target:** {{ deal_reg.close_date_target | default('—') }}
**Source:** {{ deal_reg.source | title }}
**Deal Reg ID:** `{{ deal_reg.id }}`

---

## Decision

{% if routing_decision == 'approve' %}
### Approved

**Assigned Channel Manager:** {{ assigned_channel_manager_email }}

{{ routing_reason }}

**Next steps for the channel manager:**
1. Contact {{ partner.primary_contact_email | default('the partner') }} within 2 business days.
2. Confirm opportunity details in Salesforce.
3. Schedule a co-sell alignment call if ARR > ${{ config.co_sell_threshold_usd | default(50000) | int | commas }}.

{% elif routing_decision == 'reject' %}
### Rejected

{{ routing_reason }}

The partner has been notified via the partner portal. If you believe this
decision is incorrect, contact the channel operations team.

{% elif routing_decision == 'conflict' %}
### Conflict Detected — Pending Mediation

A direct-pipeline Salesforce opportunity already exists for this account:
- **Opportunity ID:** `{{ conflict_details.sf_opportunity_id }}`
- **Direct AE:** {{ conflict_details.direct_ae_email | default('unknown') }}
- **Opportunity Age:** {{ conflict_details.age_days | int }} days

{{ routing_reason }}

The conflict-mediator agent is preparing a resolution proposal. You will
receive a conflict-resolution frame for review shortly.

{% endif %}

---

## Routing Details

| Dimension | Value |
|-----------|-------|
| Routing Confidence | {{ routing_confidence | title }} |
| Partner Tier | {{ partner.tier | upper }} |
| Partner Activity Score | {{ partner.activity_score | round(1) }}/100 |
| Registration Expires | {{ deal_reg.expires_at | datetimeformat('%Y-%m-%d') }} |

{% if deal_reg.notes %}
## Partner Notes

> {{ deal_reg.notes | truncate(400) }}
{% endif %}

---

*Generated by xiaoguai partner-enablement / deal-reg-router v{{ pack_version }}*
