{# packs/hr-onboarding/outputs/onboarding-report.md.j2
   Jinja2 template for the onboarding summary report.
   Rendered by the coordinator after all steps complete; sent to the
   hiring manager via the configured IM adapter.
#}
# Onboarding Report — {{ employee.name }}

**Employee:** {{ employee.name }} ({{ employee.email }})
**Start Date:** {{ employee.start_date }}
**Report generated:** {{ generated_at }}
**Overall status:** {% if all_succeeded %}✅ Complete{% else %}⚠️ Partial — see failed steps below{% endif %}

---

## Step Results

| Step | Description | Status | Output |
|------|-------------|--------|--------|
{% for step in steps %}
| {{ step.step_id }} | {{ step.description | truncate(60) }} | {% if step.success %}✅ Success{% else %}❌ Failed{% endif %} | {{ step.output | truncate(80) }} |
{% endfor %}

---

## Summary

{% if all_succeeded %}
All four onboarding steps completed successfully.

- **Accounts provisioned:** Okta, Google Workspace, GitHub
- **Meetings scheduled:** Day-1 Orientation, Manager 1:1, Team Introduction, IT Setup
- **Welcome message sent:** Feishu DM + group chat "Onboarding: {{ employee.name }}" created
- **Buddy assigned:** {{ buddy_name }} ({{ buddy_email }}) has been notified

{{ employee.name }} is ready to start. 🎉
{% else %}
The following steps **failed** and require manual follow-up:

{% for step in steps if not step.success %}
### ❌ {{ step.step_id }}

**Error:** {{ step.output }}

**Action required:** Please complete this step manually or re-trigger the
`{{ step.step_id }}` worker after resolving the issue.

{% endfor %}
The remaining steps completed successfully. {{ employee.name }}'s onboarding
is partially complete — see failed steps above for manual action items.
{% endif %}

---

*Generated by xiaoguai HR Onboarding Pack v1.3.4-prep*
*Coordinator: hr-onboarding-coordinator | Pack: packs/hr-onboarding*
