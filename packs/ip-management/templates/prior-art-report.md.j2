{# templates/prior-art-report.md.j2
   Rendered by the prior-art-discoverer agent.
   Variables: report (PriorArtReport JSON), context.application_details.
#}
# Prior Art Search Report

**Application:** {{ report.application_number }} — {{ context.application_details.title }}
**Search date:** {{ report.search_date }}
**Overall risk:** {{ report.overall_risk | upper }}
**Search adequacy:** {{ report.search_adequacy | replace('_', ' ') | title }}
**Confidence:** {{ report.confidence | title }}

> **DRAFT — Pending outside counsel review. Not a legal opinion.**

---

## Application Details

| Field | Value |
|---|---|
| Application number | {{ context.application_details.application_number }} |
| Filing date | {{ context.application_details.filing_date }} |
| Technology class | {{ context.application_details.technology_class }} |
| Inventors | {{ context.application_details.inventors | join(', ') | default('—') }} |

---

## Ranked Prior Art References

{% for ref in report.ranked_references | sort(attribute='relevance_score', reverse=True) %}
### {{ loop.index }}. {{ ref.reference_title | default(ref.reference_id) }}

| Field | Value |
|---|---|
| Reference ID | `{{ ref.reference_id }}` |
| Relevance score | {{ ref.relevance_score }} / 1.000 |
| Novelty impact | **{{ ref.novelty_impact | replace('_', ' ') | title }}** |
| Affected claims | {{ ref.affected_claims | join(', ') | default('—') }} |

**Analysis:** {{ ref.analysis }}

{% if ref.claim_narrowing_suggestion is defined and ref.claim_narrowing_suggestion %}
**Claim narrowing suggestion:** {{ ref.claim_narrowing_suggestion }}
{% endif %}

---
{% endfor %}

## Recommended Actions

{% for action in report.recommended_actions %}
- {{ action }}
{% endfor %}

{% if report.search_adequacy == 'needs_supplement' %}

> **Additional search recommended.** The references identified may not fully
> cover the claim scope.  Manual search by a patent professional is advised
> before finalising the application.

{% endif %}

---

**Distribution: IP Counsel only. Attorney-client privilege asserted.**

- [ ] Reviewed by outside counsel
- [ ] Claims reviewed for amendment opportunities
- [ ] Prior art citations prepared for IDS submission

*Draft generated by prior-art-discoverer · {{ now() | iso8601 }}*
