{# templates/renewal-decision-card.md.j2
   Rendered by the renewal-decision-recommender agent.
   Variables injected: event, recommendation, scores, context.
#}
# IP Renewal Decision — {{ event.jurisdiction }} / {{ event.patent_number | default(event.registration_number) }}

**Recommended action:** `{{ recommendation.recommendation | upper }}`
**Confidence:** {{ recommendation.confidence | title }}
**Decision deadline:** {{ event.due_date | default(event.next_renewal_date) }} ({{ event.days_until_due | default(event.days_until_renewal) }} days)

---

## Asset

| Field | Value |
|---|---|
| Kind | {{ event.ip_kind | default('patent') | title }} |
| Identifier | {{ event.patent_number | default(event.registration_number) | default('—') }} |
| Title / Mark | {{ event.patent_title | default(event.mark) | default('—') }} |
| Jurisdiction | {{ event.jurisdiction }} |
| Milestone | {{ event.milestone_label | default(event.renewal_cycle_type) | default('—') }} |
| Next renewal fee (USD) | {{ event.amount_usd | default('—') }} |

---

## Scoring breakdown

| Dimension | Raw value | Normalised score | Weight |
|---|---|---|---|
| Revenue attribution (USD/yr) | {{ recommendation.revenue_attribution_usd | default('—') }} | {{ recommendation.revenue_score }} | 50% |
| Competitive value (1–10) | {{ recommendation.competitive_value_score | default('—') }} | {{ recommendation.competitive_score }} | 35% |
| Annual maintenance cost (USD) | {{ recommendation.annual_maintenance_cost_usd | default('—') }} | {{ recommendation.cost_score }} | 15% |
| **Weighted score** | | **{{ recommendation.weighted_score }}** | |

Thresholds applied: renew ≥ 0.60 / defer ≥ 0.35 / let_lapse < 0.35.

---

## Rationale

{{ recommendation.reasoning_summary }}

{% if recommendation.data_gaps %}
**Data gaps (inputs treated as default):**
{% for gap in recommendation.data_gaps %}
- {{ gap }}
{% endfor %}
{% endif %}

{% if recommendation.sibling_context %}
**Sibling filing context:**
{{ recommendation.sibling_context }}
{% endif %}

---

## Required action

- [ ] **Approve** — proceed with `{{ recommendation.recommendation }}`
- [ ] **Override** — choose a different action and provide reason below
- [ ] **Defer** — request additional information before deciding

**Override action (if applicable):** _______________
**Override reason:** _______________
**Decided by:** _______________   **Date:** _______________

---

*Draft generated by renewal-decision-recommender · {{ now() | iso8601 }}*
*All decisions logged to ip_renewal_decisions for audit.*
