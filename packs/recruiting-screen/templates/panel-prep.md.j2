{# templates/panel-prep.md.j2
   Panel preparation guide rendered for each interviewer.
   Populated from interview-question-generator output + bias-checker output.
   Candidate name is NEVER rendered — only the opaque candidate_id token.
#}
# Panel Prep Guide — {{ job_title }} ({{ job_id }})

**Candidate**: {{ candidate_id }} *(identity kept anonymous until debrief)*
**Date generated**: {{ generated_at | datetimeformat("%Y-%m-%d") }}
**Bias audit**: {{ bias_verdict }} — all questions reviewed for neutrality.

---

## Candidate Fit Summary

- **Match score**: {{ "%.0f"|format(overall_score * 100) }}%
- **Seniority fit**: {{ score_breakdown.seniority_fit | round(2) }}
- **Required skills coverage**: {{ score_breakdown.required_skills_coverage | round(2) }}

**Key strengths**: {{ strengths | join(" · ") }}

**Areas to probe** (identified gaps):
{% for g in gaps -%}
- {{ g }}
{% endfor %}

---

## Interview Sections & Questions

{% for section in panel_sections %}
### {{ section.section }}

{% for qid in section.question_ids -%}
{% set q = questions | selectattr("id", "equalto", qid) | first %}
**{{ q.id }}** *({{ q.category }} — probing: {{ q.skill_target }})*

> {{ q.question }}

{% if q.follow_up %}*Follow-up*: {{ q.follow_up }}{% endif %}

*Strong answer looks like*: {{ q.evaluation_guide }}

{% endfor %}
{% endfor %}

---

## Interviewer Reminders

- Assess only job-relevant skills and behaviours.
- Do NOT ask about: family plans, relocation intent, age, health, religion,
  national origin, or any characteristic unrelated to job performance.
- Take structured notes using the evaluation guide above.
- Submit your debrief score within 24 hours of the interview.

---

*Generated by xiaoguai recruiting-screen pack.  {{ bias_neutral_confirmation }}*
