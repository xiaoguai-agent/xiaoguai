{# packs/travel-expense/templates/reimbursement-batch-report.md.j2
   Rendered by reimbursement-batcher when generating the HOTL approval request.
   Delivered via outputs/payroll-batch-csv (CSV attached) and shown in admin UI.
   Also used as the base for exec-monthly-te-digest when triggered at month-end.
#}
# T&E Reimbursement Batch — {{ batch.batch_reference }}

**Period:** {{ batch.period_start }} to {{ batch.period_end }}
**Generated:** {{ generated_at }}
**Status:** Pending finance approval (HOTL gate)

---

## Summary

| Metric | Value |
|--------|-------|
| **Total reimbursement (USD)** | {{ "USD {:,.2f}".format(batch.total_amount_usd) }} |
| **Employees** | {{ batch.employee_count }} |
| **Expense line items** | {{ batch.expense_count }} |
| **Expenses with waived policy flags** | {{ waived_violation_count }} |
| **FX-pending expenses excluded** | {{ fx_pending_excluded_count }} |

{% if fx_pending_excluded_count > 0 %}
> **Note:** {{ fx_pending_excluded_count }} expense(s) were excluded because their
> USD normalisation is pending a fresh FX rate. These will appear in the next
> batch once rates are available.
{% endif %}

---

## Employee Breakdown

| Employee | Expenses | Total (USD) | Waived Flags |
|----------|----------|-------------|:------------:|
{% for emp in employees %}
| {{ emp.name }} ({{ emp.employee_id }}) | {{ emp.expense_count }} | {{ "USD {:,.2f}".format(emp.total_usd) }} | {% if emp.waived_violations > 0 %}{{ emp.waived_violations }} ⚠️{% else %}—{% endif %} |
{% endfor %}

---

## Category Breakdown

| Category | Count | Total (USD) | % of Batch |
|----------|------:|-------------|:----------:|
{% for cat in categories %}
| {{ cat.category | replace("_", " ") | title }} | {{ cat.count }} | {{ "USD {:,.2f}".format(cat.total_usd) }} | {{ "%.1f%%".format(cat.pct) }} |
{% endfor %}

---

## Approve This Batch

This batch requires your explicit approval before payroll release.

**[Approve & Release to Payroll]({{ approve_url }})** &nbsp;&nbsp; **[Reject Batch]({{ reject_url }})**

The attached CSV (`{{ csv_filename }}`) is formatted for
{{ payroll_system | upper }} import. Please verify line counts before approving.

---

{% if is_month_end %}
## Monthly Executive Summary

This is the final batch for {{ month_name }} {{ year }}.
See the full executive digest at: [outputs/exec-monthly-te-digest]({{ digest_url }})
{% endif %}

**Batch ID:** `{{ batch.id }}`
**Reference:** `{{ batch.batch_reference }}`
*Generated by xiaoguai T&E Pack — reimbursement-batcher v{{ batcher_version }}*
