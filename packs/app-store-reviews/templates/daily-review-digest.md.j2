# App Store Review Digest — {{ digest_date }}

> Generated by xiaoguai app-store-reviews pack v{{ pack_version }}
> Period: {{ period_start }} → {{ period_end }} ({{ period_days }}-day window)

---

## Rating Overview

| Platform | Today's Rating | 28-day Baseline | Delta | New Reviews |
|----------|:--------------:|:---------------:|:-----:|:-----------:|
| Apple App Store | {{ apple.mean_rating | round(2) }} ⭐ | {{ apple.baseline_mean | round(2) }} ⭐ | {{ apple.delta | round(2) | prefix_sign }} | {{ apple.review_count }} |
| Google Play | {{ google.mean_rating | round(2) }} ⭐ | {{ google.baseline_mean | round(2) }} ⭐ | {{ google.delta | round(2) | prefix_sign }} | {{ google.review_count }} |
| **Combined** | **{{ combined.mean_rating | round(2) }} ⭐** | **{{ combined.baseline_mean | round(2) }} ⭐** | **{{ combined.delta | round(2) | prefix_sign }}** | **{{ combined.review_count }}** |

{% if anomalies %}
### ⚠ Anomalies Detected
{% for anomaly in anomalies %}
- **{{ anomaly.anomaly_type | replace('_', ' ') | title }}** on {{ anomaly.platform | title }}:
  {{ anomaly.description }} (z={{ anomaly.z_score | round(2) }})
{% endfor %}
{% endif %}

---

## Theme Breakdown (Today)

| Rank | Theme | Category | Reviews | Mean ⭐ | Star Impact |
|:----:|-------|----------|:-------:|:-------:|:-----------:|
{% for theme in top_themes %}
| {{ loop.index }} | `{{ theme.theme_label }}` | {{ theme.category }} | {{ theme.mention_count }} | {{ theme.mean_rating | round(2) }} | {{ theme.star_impact | round(2) }} |
{% endfor %}

---

## Top Fix Priorities

| Rank | Theme | Impact Score | Mentions | Star Impact | Tier Mix | Jira |
|:----:|-------|:------------:|:--------:|:-----------:|:--------:|------|
{% for fix in fix_priorities[:10] %}
| {{ fix.rank }} | `{{ fix.theme_label }}` | {{ fix.impact_score | round(1) }} | {{ fix.mention_count }} | {{ fix.star_impact | round(2) }} | {{ fix.tier_mix_weight | round(2) }} | {% if fix.jira_ticket_id %}[{{ fix.jira_ticket_id }}]({{ env.JIRA_BASE_URL }}/browse/{{ fix.jira_ticket_id }}){% else %}—{% endif %} |
{% endfor %}

---

## Notable Reviews

### Lowest-Rated (1–2 ⭐)
{% for review in lowest_rated[:5] %}
> **[{{ review.platform | title }} · {{ review.rating }}⭐ · {{ review.reviewed_at | date('D MMM') }}]**
> {{ review.body | truncate(300) }}
> *Theme: `{{ review.theme_label | default('unclassified') }}`*

{% endfor %}

### Highest-Rated (5 ⭐)
{% for review in highest_rated[:3] %}
> **[{{ review.platform | title }} · 5⭐ · {{ review.reviewed_at | date('D MMM') }}]**
> {{ review.body | truncate(200) }}

{% endfor %}

---

*Generated automatically by xiaoguai app-store-reviews pack v{{ pack_version }}.*
*Human review required before any response is posted to the app stores.*
