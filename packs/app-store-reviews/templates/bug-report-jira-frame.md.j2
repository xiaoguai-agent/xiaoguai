# Bug Report: {{ bug.title }}

> **Source**: App Store Reviews ({{ bug.evidence_reviews | length }} review(s))
> **Theme**: `{{ bug.theme_label }}`
> **Severity estimate**: {{ bug.severity_estimate | upper }}
> **Reproducibility**: {{ bug.reproducibility | title }}
> **Impact score**: {{ fix_priority.impact_score | round(1) }} (rank #{{ fix_priority.rank }})
> **Generated**: {{ now_iso }}

---

## Summary

{{ bug.summary }}

---

## Affected Conditions

| Field | Value |
|-------|-------|
| OS Version | {{ bug.conditions.os_version | default('unknown') }} |
| Device | {{ bug.conditions.device | default('unknown') }} |
| App Version | {{ bug.conditions.app_version | default('unknown') }} |
| Network | {{ bug.conditions.network | default('unknown') }} |

---

## Steps to Reproduce

{% if bug.steps_to_reproduce %}
{% for step in bug.steps_to_reproduce %}
{{ loop.index }}. {{ step }}
{% endfor %}
{% else %}
*Not determinable from review text. See evidence reviews below.*
{% endif %}

---

## Observed Behaviour

{{ bug.observed_behaviour | default('See evidence reviews.') }}

## Expected Behaviour

{{ bug.expected_behaviour | default('Not stated by reviewer.') }}

{% if bug.error_message %}
## Error Message

```
{{ bug.error_message }}
```
{% endif %}

---

## Impact Scoring

| Metric | Value |
|--------|-------|
| Mention count ({{ scoring_window_days }}-day window) | {{ fix_priority.mention_count }} |
| Theme mean rating | {{ fix_priority.mean_rating | round(2) }} ⭐ |
| App overall mean rating | {{ overall_mean_rating | round(2) }} ⭐ |
| Star impact | {{ fix_priority.star_impact | round(3) }} |
| Tier mix weight (pro+enterprise fraction) | {{ fix_priority.tier_mix_weight | round(3) }} |
| **Impact score** | **{{ fix_priority.impact_score | round(1) }}** |

---

## Evidence Reviews

{% for review_id in bug.evidence_reviews %}
{% set review = reviews_by_id[review_id] %}
> **[{{ review.platform | title }} · {{ review.rating }}⭐ · {{ review.reviewed_at | date('YYYY-MM-DD') }} · {{ review.app_version | default('?') }}]**
> {{ review.body | truncate(500) }}

{% endfor %}

---

*Auto-generated by xiaoguai app-store-reviews pack v{{ pack_version }}.*
*Triage and verify before marking as confirmed.*
