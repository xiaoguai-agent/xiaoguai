{# packs/sales-qualification/templates/discovery-call-summary.md.j2
   Jinja2 template — rendered after a discovery call note is ingested.
   Used by the account executive as a structured post-call summary.
#}
# Discovery Call Summary — {{ lead.company_name }}

**Contact:** {{ lead.contact_name }} ({{ lead.contact_email }})
**Date:** {{ call_date }}
**AE:** {{ ae_name }}
**Summary generated:** {{ generated_at }}

---

## BANT Snapshot

| Dimension | Score (0–25) | Key Evidence |
|-----------|:------------:|--------------|
| Budget    | {{ bant.budget.score }}    | {{ bant.budget.evidence | first | default('—') }} |
| Authority | {{ bant.authority.score }} | {{ bant.authority.evidence | first | default('—') }} |
| Need      | {{ bant.need.score }}      | {{ bant.need.evidence | first | default('—') }} |
| Timeline  | {{ bant.timeline.score }}  | {{ bant.timeline.evidence | first | default('—') }} |
| **Total** | **{{ bant.total_score }}** | {% if bant.total_score >= bant_pass_threshold %}PASS{% else %}FAIL{% endif %} |

---

## MEDDIC Completeness ({{ meddic.completeness_pct }}%)

| Dimension          | Status | Evidence / Notes |
|--------------------|:------:|------------------|
| Metrics            | {% if meddic.metrics_complete %}✅{% else %}❌{% endif %} | {{ meddic.metrics.evidence | default('Not established') }} |
| Economic Buyer     | {% if meddic.economic_buyer_identified %}✅{% else %}❌{% endif %} | {{ meddic.economic_buyer.evidence | default('Not identified') }} |
| Decision Criteria  | {% if meddic.decision_criteria_known %}✅{% else %}❌{% endif %} | {{ meddic.decision_criteria.evidence | default('Not documented') }} |
| Decision Process   | {% if meddic.decision_process_mapped %}✅{% else %}❌{% endif %} | {{ meddic.decision_process.evidence | default('Not mapped') }} |
| Identify Pain      | {% if meddic.pain_identified %}✅{% else %}❌{% endif %} | {{ meddic.identify_pain.evidence | default('Not quantified') }} |
| Champion           | {% if meddic.champion_identified %}✅{% else %}❌{% endif %} | {{ meddic.champion.evidence | default('Not confirmed') }} |

---

## Missing Information

{% if meddic.missing_dimensions %}
The following MEDDIC dimensions are incomplete. Suggested discovery questions
for the next call:

{% for dim in meddic.missing_dimensions %}
### {{ dim | title | replace('_', ' ') }}
{% for q in meddic[dim].discovery_questions %}
- {{ q }}
{% endfor %}

{% endfor %}
{% else %}
All MEDDIC dimensions are sufficiently covered. Proceed to next action.
{% endif %}

---

## Recommended Next Action

**Action:** `{{ next_action.next_action | upper | replace('_', ' ') }}`
**Follow-up in:** {{ next_action.follow_up_in_days }} days
**Rationale:** {{ next_action.rationale }}

### Action Items

| # | Owner | Task |
|---|-------|------|
{% for item in next_action.action_items %}
| {{ item.priority }} | {{ item.owner | upper }} | {{ item.task }} |
{% endfor %}

---

*Generated by xiaoguai Sales Qualification Pack v{{ pack_version }}*
*Agents: bant-extractor → meddic-deep-dive → next-action-recommender*
