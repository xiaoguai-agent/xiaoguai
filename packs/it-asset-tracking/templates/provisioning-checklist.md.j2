{# packs/it-asset-tracking/templates/provisioning-checklist.md.j2
   Rendered by provisioning-orchestrator after new-hire asset provisioning.
   Sent to IT Slack + attached to the Jira task.
#}
# IT Provisioning Checklist — {{ employee.name }}

**Employee:** {{ employee.name }} ({{ employee.email }})
**Start Date:** {{ employee.start_date | default("TBD") }}
**Provisioning run:** {{ run_id }}
**Generated:** {{ generated_at }}
**Status:** {% if all_succeeded %}All items provisioned{% else %}Partial — see gaps below{% endif %}

---

## Hardware

| Item | Model | Serial | Status | Notes |
|------|-------|--------|--------|-------|
{% if hardware.asset_id %}
| Laptop | {{ hardware.model }} | {{ hardware.serial_number | default("TBD") }} | {{ hardware.status }} | Assignment ID: {{ hardware.assignment_id }} |
{% else %}
| Laptop | — | — | **GAP — no matching laptop in stock** | Nearest alternative: {{ hardware.nearest_alternative | default("none") }} |
{% endif %}

## Software Bundle

| Application | Seats Available | Assignment | Status |
|-------------|----------------|------------|--------|
{% for item in software_bundle %}
| {{ item.product_name }} | {{ item.available_seats | default("?") }} | {% if item.assignment_id %}✅ Assigned ({{ item.assignment_id }}) {% else %}{% if item.gap %}⚠️ No seats available{% else %}❌ Not found in inventory{% endif %}{% endif %} | {{ item.status }} |
{% endfor %}

{% if software_gaps %}
### Software Gaps — Manual Action Required

{% for gap in software_gaps %}
- **{{ gap.product_name }}**: {{ gap.reason }}
{% endfor %}
{% endif %}

---

## IT Tasks (Jira: {{ jira_task_key | default("not yet created") }})

- [ ] Image laptop with standard OS build
- [ ] Apply MDM enrollment profile
- [ ] Verify BitLocker / FileVault encryption active
- [ ] Apply asset label with serial number
- [ ] Ship to employee address or stage for in-person pickup
- [ ] Confirm delivery with employee and mark Jira task resolved

---

## Onboarding Log

Provisioning outcome written back to onboarding_audit_log:
- **Step ID:** it_asset_provisioning
- **Success:** {{ overall_success }}
{% if not overall_success %}
- **Errors:** {{ error_summary }}
{% endif %}

---

*Generated by xiaoguai IT Asset Tracking Pack v1.0.0*
*Agent: provisioning-orchestrator | Run: {{ run_id }}*
