{# jtbd-board.md.j2
   Renders the JTBDMap as a structured JTBD board.
   JTBD format: Christensen "When …, I want to …, so I can …"
   Variables: jtbd_map, run
#}
# Jobs to Be Done Board: {{ run.project_name }}

> **Research run**: `{{ run.id }}`
> **Total jobs identified**: {{ jtbd_map.total_jobs }}
> **JTBD format**: Christensen — "When [situation], I want to [motivation], so I can [outcome]."
> *Auto-generated by xiaoguai product-research pack. Review before sharing.*

---

{% for job in jtbd_map.jobs | sort(attribute="frequency", reverse=true) %}
## {{ loop.index }}. {{ job.id | upper }}

> **"{{ job.statement }}"**

| Field | Value |
|---|---|
| Situation | {{ job.situation }} |
| Core motivation | {{ job.motivation }} |
| Expected outcome | {{ job.outcome }} |
{% if job.emotional_job %}| Emotional job | {{ job.emotional_job }} |{% endif %}
{% if job.social_job %}| Social job | {{ job.social_job }} |{% endif %}
| Participants | {{ job.participant_count }} |
| Frequency | {{ job.frequency }} mentions |
| Supporting themes | {{ job.supporting_theme_ids | join(", ") }} |

---
{% endfor %}

{% if jtbd_map.unmapped_theme_ids %}
## Unmapped Themes

The following themes did not cluster cleanly into a JTBD statement.
Review manually:

{% for tid in jtbd_map.unmapped_theme_ids %}
- `{{ tid }}`
{% endfor %}
{% endif %}

*Generated by xiaoguai product-research pack v{{ pack_version }}.*
