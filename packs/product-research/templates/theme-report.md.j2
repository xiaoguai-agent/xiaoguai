{# theme-report.md.j2
   Renders the full ThemeSet as a structured markdown report.
   Variables: theme_set, run
#}
# Theme Report: {{ run.project_name }}

> **Research run**: `{{ run.id }}`
> **Sources**: {{ run.sources | join(", ") }}
> **Participants**: {{ theme_set.total_participants }}
> **Segments reviewed**: {{ theme_set.total_segments_reviewed }}
> **Generated**: {{ run.generated_at }}
> *Auto-generated by xiaoguai product-research pack. Review before sharing.*

---

## Summary

{{ theme_set.themes | length }} recurring theme(s) identified across {{ theme_set.total_participants }} participants.
{% if theme_set.singletons %}
{{ theme_set.singletons | length }} singleton quote(s) did not meet the 2-participant threshold and are listed at the end.
{% endif %}

---

## Themes

{% for theme in theme_set.themes | sort(attribute="frequency", reverse=true) %}
### {{ loop.index }}. {{ theme.label }}

| Field | Value |
|---|---|
| Theme ID | `{{ theme.id }}` |
| Participants | {{ theme.participant_count }} |
| Frequency | {{ theme.frequency }} mentions |
| Sources | {{ theme.source_files | join(", ") }} |

**Representative verbatims:**

{% for v in theme.verbatims[:5] %}
> "{{ v.quote }}"
> — `{{ v.source_ref }}` (participant `{{ v.participant_id }}`)

{% endfor %}

---
{% endfor %}

{% if theme_set.singletons %}
## Singletons (< 2 participants — for review only)

{% for s in theme_set.singletons %}
- `{{ s.source_ref }}` ({{ s.participant_id }}): "{{ s.quote }}"
{% endfor %}
{% endif %}

*Generated by xiaoguai product-research pack v{{ pack_version }}.*
