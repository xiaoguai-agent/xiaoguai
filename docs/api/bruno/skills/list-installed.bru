meta {
  name: List Installed Skill Packs
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/skills/installed
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  tenant: tenant_acme
}

assert {
  res.status: eq 200
  res.body: isArray
}
