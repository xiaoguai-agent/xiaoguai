# Session handoff — 2026-05-23

Use this file as the entry point when picking up work after a context
reset. Skim it in order and you'll have everything you need to continue.

## 1. Where things are

| What                 | Path                                                              |
|----------------------|-------------------------------------------------------------------|
| **Repo (work here)** | `/Users/zw/testany/myskills/xiaoguai`                             |
| Design workspace     | `/Users/zw/testany/myskills/xiaoguai-agent-design` (docs only)    |
| Remote              | `https://github.com/xiaoguai-agent/xiaoguai`                       |
| Latest tag           | `v0.6.1` — per-request RLS enforcement                            |
| Previous tag        | `v1.0`                                                             |

The design workspace under `myskills/xiaoguai-agent-design` is a thin
shell — only `docs/` and `README.md`. **Always `cd` into
`myskills/xiaoguai` for any real work.**

## 2. Milestone ladder — all green and tagged

```
v0.1.0-bootstrap   workspace + CI + license + storage skeleton
v0.5.1             storage + auth + RBAC + audit hmac chain
v0.5.2             multi-backend LLM router + circuit breaker + token usage
v0.5.3             MCP stdio client + supervisor + admin CLI
v0.5.4             ReAct agent loop + parallel tool dispatch + cancel
v0.5.5             xiaoguai-api: 5 REST endpoints + SSE streaming
v0.5.6             CLI remote mode + xiaoguai-core serves the router
v0.6               auth extractor (TokenValidator) + /v1/mcp/servers
v0.7               IM gateway crate + Feishu adapter (inbound only)
v0.8               Chat UI MVP (@xiaoguai/shared + chat-ui vite/react)
v0.9               admin UI scaffold + docker-compose + Dockerfile
v1.0               Helm chart + multi-arch release + cosign + SBOM +
                   compliance docs + operator runbook + quickstart
v0.6.1             per-request RLS enforcement: repos take
                   tenant: Option<&str>, run SET LOCAL via shared helper,
                   new e2e test under non-superuser PG role
```

**Tree state at v0.6.1:** `cargo test --workspace` → **162 passed / 0
failed / 54 ignored** (the +3 are the new RLS-isolation tests +
tenant_ctx doctest). `cargo clippy --workspace --all-targets -- -D
warnings` and `cargo fmt --all -- --check` both clean.

## 3. What's already shipped — quick map

### Backend (Rust workspace, all crates under `crates/`)

- `xiaoguai-types` — domain types + ID newtypes.
- `xiaoguai-config` — Settings (server/database/cache/auth/audit).
- `xiaoguai-storage` — sqlx + Pg repos (session/message/tenant/user/
  llm_provider/mcp_server/token_usage) + Valkey cache + RLS-aware
  migrations.
- `xiaoguai-audit` — `ChainedAudit` (HMAC).
- `xiaoguai-auth` — `JwtValidator` (RS256/ES256, JWKS cache) + Casbin.
- `xiaoguai-llm` — `LlmBackend` trait + Ollama / OpenAI-compat / Mock
  backends + `LlmRouter` + `Breakers`.
- `xiaoguai-mcp` — `McpClient` trait + `StdioMcpClient` (rmcp 1.7) +
  `McpSupervisor`.
- `xiaoguai-agent` — `Toolbox`, `ReactAgent::run_stream`,
  `AgentEvent` enum, sliding-window history.
- `xiaoguai-api` — axum router, 6 endpoints (healthz + 5 v1 routes),
  SSE encoder, `AppState`, `auth::TokenValidator` trait + middleware,
  `serve_with_state` for binaries + tests.
- `xiaoguai-im-gateway` — `ImProvider` trait + `mount_feishu` axum
  helper.
- `xiaoguai-im-feishu` — Feishu events v2 signature verify + parser.
- `xiaoguai-cli` — `xiaoguai chat / provider / mcp / remote`
  subcommands.
- `xiaoguai-core` — production binary; `smoke` + `serve` (actually
  binds the router on `settings.server.host:port`).

### Frontend (pnpm workspace under `frontend/`)

- `@xiaoguai/shared` — full TS mirror of backend wire types +
  `XiaoguaiClient` (REST + SSE parser → typed `AgentEvent`).
- `@xiaoguai/chat-ui` — vite + react + react-router: session list +
  streaming chat page + tool-call viz + cancel.
- `@xiaoguai/admin-ui` — MCP-servers pane wired against
  `/v1/mcp/servers`; tenants / providers / audit are placeholders
  pointing at the v0.6.1 backlog.

### Ops (`deploy/`)

- `Dockerfile` — multi-stage (rust:slim → distroless cc), runs as
  `nonroot`.
- `docker-compose.yml` — pg 16 + valkey 8 + core. Ollama service
  commented out as opt-in.
- `helm/xiaoguai/` — chart with deployment / service / ingress / hpa /
  helpers, secret-ref pattern (`xiaoguai-database/-cache/-auth/-audit`),
  distroless-friendly securityContext.

### CI (`.github/workflows/`)

- `rust.yml` — fmt + clippy + build + test on every push.
- `frontend.yml` — pnpm lint + typecheck.
- `deny.yml` — cargo-deny on every push.
- `release.yml` — fires on `v*` tags: multi-arch buildx push to
  `ghcr.io/xiaoguai-agent/xiaoguai` + cosign keyless signature + parallel
  cargo-cyclonedx SBOM upload.

### Docs

- `docs/architecture/2026-05-21-design.md` — full design doc.
- `docs/plans/2026-05-21-v0.5-inner-loop.md` — every v0.5.x sub-milestone
  closed out with acceptance numbers.
- `docs/plans/2026-05-22-v0.6-to-v1.0.md` — v0.6/0.7/0.8/0.9/1.0 carve.
- `docs/user-guide/quickstart.md` — 5-min docker-compose → first SSE
  chat.
- `docs/compliance/dengbao-2.0-l3/README.md` — 等保 2.0 三级 self-check.
- `docs/compliance/gdpr/dpia-template.md` — Article 35 DPIA template.
- `docs/runbooks/operator.md` — day-2 procedures.

## 4. Backlog by milestone (what's intentionally NOT done)

Read these as "the next session's todo list". Each lives in the milestone
plan doc with full context.

### v0.6.1 — DONE (per-request RLS)

See `docs/plans/2026-05-23-v0.6.1-rls.md`. Repo trait methods now take
`tenant: Option<&str>` and run inside a transaction that calls
`set_config('app.current_tenant_id', $1, true)`. The new
`xiaoguai-storage/tests/rls_isolation.rs` proves tenant boundaries hold
under a non-superuser PG role.

### v0.6.2 — backend hardening finishers (next up)

- WebSocket fallback `/v1/sessions/:id/ws` for browsers that block SSE.
- OpenAPI spec via `utoipa` — would touch every handler annotation.
- `/v1/admin/tenants` + `/v1/admin/audit` endpoints (admin-ui already has
  placeholder panes waiting for them).
- `GET /v1/sessions?user_id=...` listing — chat-ui sidebar is in-memory
  right now.
- Wire the real `JwtValidator` into `xiaoguai-core serve` behind a
  `XIAOGUAI_AUTH_REQUIRED` settings knob (the trait + middleware are
  already there; just no boot wire-up).
- Casbin per-route enforcement layer.
- Rate-limit middleware per tenant.
- LlmRouter wired into `AppState` (currently `MockBackend` by default in
  `xiaoguai-core serve`). Read `llm_providers` on boot.
- `provider remove --tenant` / `mcp remove --tenant` for scoped admin
  deletes.
- `UserRepository` migration to the shared `begin_tenant_tx` helper.

### v0.7.1 — IM finishers

- Real Feishu OpenAPI reply (currently `ReplySink::Stub` /
  `Recording`). Needs `tenant_access_token` caching.
- Wire the full `ReactAgent` run + persistence inside the Feishu
  webhook handler (v0.7 only echoes — proves wiring).
- DingTalk + WeCom providers implementing `ImProvider`.
- Interactive cards (rich messages w/ tool-call viz).
- Long-task handoff (reply > Feishu hard size cap).

### v0.8.1 — Chat UI polish

- Real design system (currently minimal `styles.css`).
- MCP studio (browse tools, dry-run a single call).
- Multi-modal blocks (images, files).
- Auth flow (OIDC) in the browser — depends on v0.6.1.

### v1.0+ / v1.1

- Conversation fork / branching.
- Public-cloud LLM provider configs in the registry (Anthropic /
  Gemini / 通义 / DeepSeek / 智谱).
- Self-read `/usage` endpoint.
- MCP marketplace (community registry).
- HA: PG replica + Valkey cluster.
- RAG module + vector store.
- Multi-agent orchestration.

### MCP supervisor backlog (was v0.5.3.1)

- SSE + streamable-HTTP transports (only stdio shipped).
- Sandbox: cgroup v2 + seccomp + netns (Linux-only).
- Health-check 30 s ping + Prometheus metrics.
- Default-deny netpolicy.
- Supervisor idle-timeout / LRU / crash-restart.
- Startup auto-populate from PG `mcp_servers`.

### Compliance / packaging backlog

- Bare-metal tarball + systemd unit + uninstall.
- `pip` wheel for the CLI (Python wrapper).
- Cargo-deny / Snyk full pipeline (currently only `deny.yml` advisory).
- Actually run a 测评 with an MPS-accredited assessor.

## 5. How to resume next session

Recommended opening prompt:

```
继续。先 cd /Users/zw/testany/myskills/xiaoguai 看 git log -5，
然后读 docs/HANDOFF-2026-05-23.md。我想做 <pick from backlog above>。
```

If you want to pick the highest-leverage next slice:

1. **v0.6.2 Casbin + LlmRouter + JwtValidator wire-up** — the
   remaining strands of the original v0.6.1 plan now that RLS itself
   has shipped. Smaller risk per strand.
2. **v0.7.1 Feishu real reply** — turns the demo into an actual IM bot.
3. **v0.8.1 design system pass** — fastest way to make a screencast that
   doesn't look like a Hacker News post.

## 6. Working conventions to keep

- Commit per logical task with `feat/fix/test/docs(vX.Y.Z): ...`.
- Every milestone closes with a doc update + `cargo test --workspace`
  number recorded + `clippy -D warnings` + `fmt --check` clean.
- Each milestone tagged + pushed: `git tag vX.Y && git push origin
  main && git push origin vX.Y`.
- Always provide a deferral list in the milestone plan doc — leave the
  tree shippable at every tag.
- Tests live next to the code they cover (`tests/` for crate
  integration; `#[cfg(test)] mod tests` for unit). Use scripted
  `MockBackend` + in-memory repos in `tests/common/`.
- Frontend changes: keep `@xiaoguai/shared` as the single source of
  truth for wire types — both chat-ui and admin-ui import from it.

## 7. Known sharp edges

- `xiaoguai-core serve` currently ships **MockBackend** by default. A
  real session would respond with the same canned string. Document this
  to users; fix in v0.6.2 by reading the `llm_providers` table on boot.
- `auth: None` is the default in dev. Set
  `XIAOGUAI_AUTH_REQUIRED=true` + provide JWKS to flip to prod mode —
  but the boot wiring for that knob isn't there yet either.
- Feishu integration's signature algorithm matches the *common* spec
  variant; some Feishu apps use a slightly different scheme depending on
  encryption settings. Real-world testing pending.
- The 51 ignored tests are mostly testcontainer-based PG e2e — they pass
  locally if you have Docker but are not part of CI's fast path.
