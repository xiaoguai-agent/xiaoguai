# Session handoff — 2026-05-26 (v1.4.0 + public release)

Resume point after the v1.4 deferred-merge-wave session. Repo is **clean,
fully pushed, and PUBLIC**. `main` == `origin/main`. Latest release: **v1.4.0**.

## What shipped this session

- **v1.4 deferred merge wave → main** (PR #17, fast-forward, 23 commits):
  integrated the 5 deferred branches — `xiaoguai-memory` (pgvector recall),
  multi-workspace concept, `xiaoguai-tasks` (kanban board + auto-dispatcher),
  `xg tasks` CLI, and `GET /v1/tenants/:id/config`. Migrations now
  `0016_personas → 0017_workspaces → 0018_tasks → 0019_memories`.
- **CI brought fully green for the first time** (it was never green — always
  died at fmt). Fixes, in order: repo-wide `cargo fmt`; `clippy --all-targets
  -D warnings` (workspace pedantic allow-list + 2 dead-code fixes); cargo-deny
  (3 unmaintained advisory ignores); **MSRV/toolchain reconciled to 1.88**;
  protobuf 2.28 removed (RUSTSEC-2024-0437, closed #16); **AWS S3 audit export
  + Snyk removed** (private-first); Pact npm lockfiles; sd-notify Linux-only
  compile + clippy fixes; runner **disk-space** step (cold full build OOM'd
  disk); 3 stale tests repaired (anomaly accuracy-eval intent, skills catalog
  7→9, ar-collections obsolete watch-spec schema).
- **License**: BUSL-1.1, Licensor = **Zhou Wei (zhouwei008@gmail.com)**,
  grant tightened — free for dev/test/eval/personal/academic; **any production
  use (incl. private/on-prem/internal) requires a commercial license**. Change
  Date 4 yrs → Apache-2.0. Synced to Go/Java/TS SDK LICENSE files.
- **Repo flipped PUBLIC** after a clean gitleaks full-history scan (2 findings
  were test fixtures, allowlisted in `.gitleaks.toml`). Bonus: public = free
  unlimited GitHub Actions (the billing block that started this is moot).
- Cleanup: ~150 merged branches deleted, dead agent worktrees removed,
  accidental `.claude/worktrees/` gitlinks untracked + gitignored.

## The ONE remaining tracked task

**Dependabot — 14 open PRs** (aws-sdk-s3 #6 auto-closed when the dep was removed).
Now unblocked (main is fmt/clippy/MSRV clean → they pass the gate after rebase):

- **Low-risk, merge directly**: #1-5 GitHub Actions bumps; #9/#10 testcontainers
  minor bumps.
- **Major bumps needing API migration (one at a time, compile-check each)**:
  `rand 0.8→0.10` (#14), `opentelemetry 0.27→0.32` (#7) + semconv (#11),
  `tantivy 0.22→0.26` (#13), `jsonwebtoken 9→10` (#15), `aes 0.8→0.9` (#8),
  `cipher 0.4→0.5` (#12).

## pi/Hermes feature roadmap (researched, not started)

Goal: **a good local agent for enterprise private deployment** (pi = OpenClaw's
minimal agent; Hermes = Nous Research's autonomous agent). xiaoguai already has
RBAC/Casbin, HMAC audit, OTel, MCP (consume+serve), memory, 7 IM gateways,
HotL governance, skill-packs. Gaps to add:

- **Tier 1 (private-deploy wedge)**: Ollama/llama.cpp as first-class *default*
  backend + documented air-gapped mode; single self-contained binary (Rust +
  Postgres only); PII detection/redaction in traces.
- **Tier 2 (parity)**: programmatic tool calling (`execute_code` to collapse
  tool pipelines); agent-authored skills gated by HotL; session compaction.
- **Tier 3 (polish)**: OAuth 2.1 PKCE for authed MCP servers; compliance report
  export (SOC2/GDPR/HIPAA) from the audit chain.

Pitch: *Hermes-class capability + pi's local-first, wrapped in the RBAC/audit/
HotL governance enterprises need for private deployment.*

## Deferred (pre-existing CI debt, non-blocking — "option A")

Frontend/E2E CI jobs still red, unrelated to the wave: Pact (python/go SDK
consumers), Playwright ×3, k6 smoke, manifest validators (packs/recipes/
watchers/hotl). They need real runtime envs; track separately.

## Key facts to not re-learn

- **Toolchain pinned 1.88** (`rust-toolchain.toml`); MSRV `rust-version=1.88`.
  Don't reintroduce deps that force 1.91 (that's why AWS SDK was dropped).
- CI `rust.yml` runs `clippy --all-targets -D warnings` + `build --all-targets`
  + `test --workspace`. DB tests are `#[ignore]`'d (run in the migrations-smoke
  job via `--include-ignored`), so they don't gate build-test.
- `.gitleaks.toml` allowlists two test fixtures (wecom sample key, jwt test key).
- Develop on macOS; `#[cfg(target_os = "linux")]` code (sd_notify_bridge.rs,
  prometheus process collector) only compiles on CI — check those there.
