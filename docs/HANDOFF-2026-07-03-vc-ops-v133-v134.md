# HANDOFF — VC 运维助手 + Fable 全项目体检 + v1.34 治理切片 (2026-07-03)

> Durable checkpoint following `HANDOFF-2026-06-08.md` era docs (and the
> memory-side session files up to 2026-06-29). Two days' arc: Cherry-Studio IA
> polish (#387) → VC-ops assistant (#388) → whole-project Fable review (fixes
> merged) → v1.34 governance slice (#394). **Unreleased on main: everything
> since v1.32.0 — the owner explicitly HELD the release** (tag = owner's call).
>
> **main tip at handoff: `6ba60d5` (#394). 0 open non-dependabot PRs.**

## 1. Shipped to main (chronological)

- **#387 chat-ui UX polish** (`c7bbef5`): brand name above 助手/对话 tabs;
  assistant rows show role lines; 话题→对话; header ExpertPicker replaced by a
  read-only active-assistant display (teamId now derived via `getSessionTeam`
  on session load); model picker moved after the 执行/只读 toggle; 咨询→只读.
- **#388 VC 运维助手** (`9d22c2c`, squash of the whole feat/vc-ops-assistant
  line). Highlights:
  - **地基① persona injection**: session persona `system_prompt` → System
    message in every chat turn (was orchestrate-only). 地基② **MCP tool merge**:
    active MCP servers' tools fold into the per-turn toolbox
    (`merge_active_mcp_tools`, 6 direct unit tests) with `MutationHint`
    honored (consult layer-1 subset + layer-2 gate).
  - **vmware MCP family (zw008)**: 9 marketplace entries CORRECTED to the real
    invocation `command=vmware-<name>-mcp, args=[]` (the `vmware-<name> mcp`
    spelling never existed — caught by reading the family monorepo at
    `~/testany/myskills/vmware-skill/`; each package is CLI+MCP+skill triple,
    creds live server-side in `~/.vmware-<name>/{config.yaml,.env}` reached via
    the PATH+HOME passthrough, so env_keys=[] is correct and **no vCenter
    cred ever touches xiaoguai's DB**).
  - **VmwareStarter card** (chat welcome, VM-ops persona detected by name):
    monitor-first ordering, read-only/ops badges, one-click install, honest
    `uv tool install` + vCenter-config prerequisite note. 8 component tests.
  - **Security batch from the Fable review** (see §2): repaired cargo-deny
    gate + 4 RUSTSEC clears + aes-gcm 0.11 / tera 2.0 migrations
    (superseded Dependabot #384/#383), quick-xml 0.41, wasmtime-wasi 45.0.3.
  - docs: `docs/vc-ops-assistant.md` (setup/usage/honest limits).
- **#394 v1.34 governance slice** (`6ba60d5`):
  - **persona `tool_allowlist` ENFORCED per turn** (was declared-but-unwired):
    persona fetched once pre-layering; `apply_persona_allowlist` narrows the
    merged base BEFORE loop/consult layering (loop tools layered after —
    unbreakable; consult gate keys on the narrowed base). `None`=unrestricted
    same-Arc fast path; `Some([])`=deny-all. `Toolbox::filtered` added.
    Admin-ui already had the CSV editor → feature is end-to-end usable.
  - **Turnkey 「VMware 运维助手」persona** seeded at boot,
    create-if-NEVER-existed (archived rows count — owner deletion is final).
    Prompt = family safety rules (只读优先/有就是有/变更三步走/凭据安全).
  - Marketplace: + `vmware-debug`, + `vmware-log-insight` (read-only, 1.6.1,
    facts verified against the monorepo). vmware total = 11.

## 2. Whole-project Fable review (owner: 先不发版, check 质量/遗漏/安全)

3 parallel evidence-required reviewers + spot-checks. Verdict: **no
CRITICAL/HIGH real bugs**; strong posture (SEC-03 env scrub, owner-auth,
fail-closed HotL, safeHref, no secrets in git). Real findings, all fixed:

- **cargo-deny gate had been DARK since 2026-07** (the
  EmbarkStudios/cargo-deny-action@v2 container can't honor the 1.93 toolchain
  pin) — every PR showed a tool error instead of results. Fixed by running
  cargo-deny on the runner (dtolnay + taiki-e idiom); a second fix:
  `--all-features` must precede `check`. The repaired gate then surfaced
  **4 hidden RUSTSEC advisories**: wasmtime-wasi FilePerms bypass (relevant —
  mcp-exec-wasm runs untrusted WASI; → 45.0.3), quick-xml ×2 DoS (relevant —
  im-wecom parses external WeCom XML; → 0.41), ttf-parser unmaintained
  (no safe upgrade → documented deny.toml ignore).
- 3 small code fixes (workspace.rs child-reap after timeout kill, ChatPage
  bubble React keys `messageId ?? temp-i`, SidebarExtras interpolate()).
- Reviewer-fabrication tally this arc (grep-verify EVERYTHING): "no MCP mode
  in the vmware series" (wrong — I had only read the skill cache, not the
  monorepo), "merge_active_mcp_tools untested" (wrong — 6 tests exist),
  "tera unused" (wrong — `[[test]]` target lives under `packs/`, outside a
  crates/-scoped grep), crate count 38 (it's 37).

## 3. In flight at handoff

- **Dependabot: FULLY CLEARED** (post-handoff, same loop). #383/#384 closed
  (superseded by our migrations); #379/#391 self-closed (dependabot regrouped);
  **#380 (@types/node 26.1.0) + #382 (rustyline 18) + #396 (cargo minor ×4) +
  #395 (npm minor ×4, after a lockfile-conflict rebase) ALL MERGED** — main
  tip `ea235fc`, **0 open PRs**.
- **RELEASE IS HELD** by the owner. Unreleased on main since v1.32.0:
  #387 + #388 + review fixes + #394 (+ any dep merges). Proposed tag: v1.33.0
  (or v1.34.0 given the governance slice) — **owner decides, do not tag**.

## 4. Local test env (:7600)

- `/tmp/xiaoguai-test/` was WIPED by macOS tmp cleanup mid-session (the old
  data.db with the owner's MiniMax key is gone — owner must re-enter the key;
  consider moving the test env to `~/.xiaoguai-test/` to survive cleanup).
- Rebuilt + running: debug binary from #394 content, config
  `/tmp/xiaoguai-test/config.yaml` (owner/test1234, sqlite at
  `/tmp/xiaoguai-test/data.db`, static_dir `web/`), launched detached
  (`perl setsid`, log `/tmp/xiaoguai-test/serve.log`) with
  `XIAOGUAI_AUDIT_SIGNING_KEY=dev-only…` and
  `XIAOGUAI_MCP_TRUST_READ_ONLY_HINTS=vmware-monitor,vmware-debug,vmware-log-insight`.
  Verified live: chat+admin 200, seeded persona present, 11 vmware entries.
- Restart pattern: `pkill -9 -f "config.yaml serve"` (NOT the xiaoguai.*serve
  pattern), then the perl-setsid launch above.

## 5. Owner-only tasks (told to owner; standing)

1. Re-enter a quota'd MiniMax key (`sk-cp-…`, china endpoint) in admin →
   模型 — the old key 429'd (quota) and the DB reset lost it anyway.
2. Live VC-ops E2E (the ONLY unverified link): `uv tool install
   vmware-monitor` → `~/.vmware-monitor/{config.yaml,.env}` (chmod 600) →
   self-check `vmware-monitor-mcp` → :7600 pick 「VMware 运维助手」→ starter
   card install → ask a real inventory question against their vCenter.
3. Decide the release tag; prioritize v1.34 leftovers.
4. TestPyPI OIDC (long-parked, PyPI-account-owner only).

## 5b. Expert enablement gate + rmcp 2.1 (later same session)

- **#397 (merged, `62d4acb`)** — **expert prerequisite gate**. An expert persona
  is selectable only once its prerequisites are met; a not-ready one shows in the
  picker but is LOCKED (unmet-groups hint + 「去安装」CTA). Backend
  `GET /v1/experts` (`xiaoguai-api::expert_readiness`) + `catalog/
  expert_prerequisites.json`: experts → required OR-groups + optional add-ons;
  two requirement kinds — `mcp` (marketplace slug, satisfied by a live
  mcp_servers row) and `package` (host lib/CLI, satisfied by `command -v`).
  Pure `compute_readiness()` (7 tests). 3 experts: VMware 运维 (policy pkg AND
  monitor|aiops), VMware 网络运维, 数据分析 (postgres|sqlite). `persona_seed`
  generalized to seed all 3. chat-ui gates the picker (fail-OPEN). Docs
  `docs/expert-enablement.md`.
- **#399 (merged, `c0b4590`)** — **per-expert setup page** `/experts/:key`. The
  locked CTA deep-links here (was the wrong /skills page). MCP items install
  INLINE (installMarketplace → readiness refetch → unlock); package items show
  the host `uv tool install` command + copy + honest probe status (no fake
  install button); offline_hint surfaced. Every ✓ comes only from the server's
  live readiness (anti-hallucination).
- **#400 (merged, `17818ae`)** — **rmcp 1.8 → 2.1** (superseded Dependabot #398).
  Content-model refactor: `Content`/`RawContent` → `ContentBlock` (match `c`, not
  `c.raw`); `ToolsCapability`/`ResourceContents` now `#[non_exhaustive]` (build
  via Default + field-set); streamable-http split into its own feature
  (`transport-streamable-http-client-reqwest`). 7 crates touched,
  behavior-preserving; mcp/exec/api 763 tests green; serve boot smoke passed.

**main tip now `17818ae`, 0 open PRs. Unreleased since v1.32.0 = 12 commits.**
⚠️ Accidental host mutation: `uv tool install vmware-policy` ran via a backtick
in a `git commit -m` (zsh command-substitution) — `vmware-audit` is now on this
host's PATH (harmless; it's the real family base lib). Lesson: never put
backticks in `git commit -m "…"`; use `-F <file>`.

## 6. v1.34+ backlog (not started)

- VmwareStarter **readiness panel** (per-row 已连接/未运行 needs a
  supervisor-status field on the servers list API — backend+frontend slice).
- MCP HTTP/SSE transport; admin-ui vCenter credential UX (deliberately
  server-side-only today).
- Per-turn MCP merge integration test through the full run_turn pipeline
  (unit coverage exists; glossary_injection harness could observe tools).
- Move the local test env out of /tmp (survive macOS cleanup).
