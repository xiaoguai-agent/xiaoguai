# HANDOFF — "do everything" batch + v1.13.0 + CI runner-death hunt (2026-06-08)

Durable, repo-committed checkpoint. Supersedes
`HANDOFF-2026-06-07-backlog-burndown.md` (whose follow-ups are all closed).

## 1. Released

- **v1.12.0** (2026-06-07): backlog burndown #231–#237 (eval CI gate, router
  TTFB metric, `agent.run` audit, H1 long-session persistence fix, guards).
- **v1.13.0** (2026-06-08): `xiaoguai schedule` CLI (#241), HotL lifecycle
  closure — phantom-row→404 / 409-terminal / timeout terminalisation (#242),
  model-label cardinality guard (#239), k6 root-cause fix (`/data` mountpoint
  ownership in deploy/Dockerfile — green first time since the DEC-033 pivot)
  + Playwright project-matrix fix (#240). Both verified on PyPI + deb/rpm +
  tarball + sigstore.

## 2. /loop — L1 SHIPPED (merged to main)

Design repo (`xiaoguai-agent-design`, private) main `d7db8ed`: **DEC-039** +
`lld/lld-loop.md` (**LLD-LOOP-001**).

**L1 merged 2026-06-08 — PR #244 (squash `723c7a3`).** Two commits:

- **`run_turn` extraction + per-session turn lock.** The turn pipeline
  (persist→history→identity→enforcer→`run_streamed`→finalize) moved out of
  the `send_message` SSE handler into shared `xiaoguai_api::turn::run_turn`
  (the route is now a thin SSE adapter). `CancelRegistry::register` →
  `try_begin_turn` → RAII `TurnGuard`: an occupied entry is refused
  (route → 409, loop tick → skip). This fixed the REAL latent race where
  `register()` silently evicted the prior token and two concurrent turns
  corrupted each other's finalize/persist. Side fix: a HOTL-deny early
  return no longer leaks the cancel token.
- **LoopController + storage + REST + CLI.** `0029_loops.sql` (partial
  unique index = one live loop/session) + `LoopStore`/`SqliteLoopRepository`
  (guard-based UPDATEs) + `LoopController` (per-row driver tasks + boot
  replay, mirrors the HotL decision-registry) + `POST/GET/DELETE /v1/loops`
  + `xiaoguai loop create|list|show|cancel`. Failure backoff
  (`interval × 2^failures`), auto-`failed` at 5 consecutive failures,
  session-gone → `cancelled`, budget trips → `budget_exhausted`, each with
  a teaching final session message. Audit `loop.*`; ticks tagged
  `initiator:loop` + `loop_id`. Wired in `run_serve` from an `AppState`
  clone with `loops=None` so a tick never re-enters the controller.
- Gates green: `turn_lock` + `loops_repo` (8) + `loops_controller` (9);
  clippy `-D warnings`; `cargo fmt`; workspace 2023 pass. Two code-review
  passes — the two subtle races (cancel-as-failure, double-audit) verified
  safe.

**Deferred (per LLD §9):**

- **L2** — chat slash-command parser (none exists today: only the REPL's
  hard-coded `/exit|/quit` — greenfield), `loop_done`/`loop_pause` tools,
  final-summary message, parked-tick visibility in the pending list.
- **L3** — dynamic pacing (`loop_next_tick`) + token budget. No
  session-attributed usage accounting exists yet (`token_usage.session_id`
  never populated; `ResolveCtx.session_id` has zero callers outside
  xiaoguai-llm). Build attribution before `max_total_tokens`.
- Full phasing L1–L3 in the LLD §9.

## 3. CI: the runner-death war (6 rounds, partially won)

Symptom: "runner lost communication" at hang+45 min, killing the gate for
days. Full forensics log: **issue #243** + memory `ci-gotchas`.

- **KILLED: the per-crate rustc rebuild hang.** `cargo test -p` re-resolves
  features vs `--workspace` → silent recompiles; one such rustc spun forever.
  Fix: **cargo-nextest archive** — build once (workspace unification), per-
  crate steps only run (2-3 s each). Doctests get their own step.
- **QUARANTINED, NOT root-caused: a test-execution hang in
  `xiaoguai-mcp-exec`.** Defeats every in-job containment (cgroup jail,
  swap=0, nested timeouts, file-redirect — even kill-the-waiter never
  returned control). Now in its own `continue-on-error` job: poll-don't-wait
  execution + git-push beacon (`refs/heads/ci-beacon-<run_id>`) collects
  evidence on every recurrence. Local clue: nextest flags
  `exec::tests::stderr_is_redacted_when_configured` as LEAKY ~1/7 runs.
- Gate shape now: build-test (35 crates via archive + doctests + eval) +
  quarantined mcp-exec + migrations smoke. All green on main `2d70268`.

## 4. Open items

1. **/loop L2 + L3** (L1 shipped — see §2). L2 = chat slash-command parser
   (greenfield) + `loop_done`/`loop_pause` tools + parked-tick visibility;
   L3 = dynamic pacing + token budget (needs session-attributed usage
   first).
2. Playwright ~25 stale specs (pre-pivot assumptions; chromium passes 16;
   the 3 browser shards are red on every PR incl. #244 — NON-blocking, gate
   is Build-and-test). Forensics/artifacts from #240 make the rewrite
   tractable.
3. mcp-exec quarantine hunt (#243) — check beacon refs after any red
   quarantine job: `git ls-remote origin 'refs/heads/ci-beacon-*'`.
4. Dependabot #238 (cargo minor-patch) — gates now healthy; update-branch
   then merge on green.
5. Design-repo traceability YAML drift (DEC-034..039 missing from hld.md §11
   registry) — batch catch-up.

## 5. Resume/verify

`git checkout main && git pull` (both repos);
`cargo clippy --workspace --all-targets --locked -- -D warnings`;
`cargo nextest run --workspace` (or `cargo test`); CI:
`gh run list --branch main --workflow Rust --limit 3`.
